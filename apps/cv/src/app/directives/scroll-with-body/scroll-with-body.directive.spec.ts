import { ScrollWithBodyDirective } from './scroll-with-body.directive';

describe('ScrollWithBodyDirective', () => {
    it('should create an instance', () => {
        const directive = new ScrollWithBodyDirective();
        expect(directive).toBeTruthy();
    });
});
